from math import tan, pi
circle_diameter = lambda sides, side_length: side_length / tan(pi / sides)

from math import tan, pi
def circle_diameter(sides, side_length):
  return side_length / tan(pi / sides)
