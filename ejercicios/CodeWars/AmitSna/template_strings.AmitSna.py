temple_strings = lambda obj, feature: " are ".join([obj, feature])

def temple_strings(obj, feature):
  return f"{obj} are {feature}"

def temple_strings(obj, feature):
  return obj + " are " + feature

def temple_strings(obj, feature):
  return "{0} are {1}".format(obj, feature)

def temple_strings(obj, feature):
  return " are ".join([obj, feature])

def temple_strings(obj, feature):
  return "%s are %s" % (obj, feature)
