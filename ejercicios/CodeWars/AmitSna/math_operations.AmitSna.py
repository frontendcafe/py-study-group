ice_brick_volume = lambda radius, bottle_length, rim_length: ((radius * 2) ** 2 / 2) * (bottle_length - rim_length)

def ice_brick_volume(radius, bottle_length, rim_length):
  return ((radius * 2) ** 2 / 2) * (bottle_length - rim_length)
